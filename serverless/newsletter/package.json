{
	"name": "newsletter",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy --name=newsletter-dev",
		"dev": "wrangler dev",
		"start": "npm run generate-types && wrangler dev",
		"generate-types": "wrangler types"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.38.0"
	}
}